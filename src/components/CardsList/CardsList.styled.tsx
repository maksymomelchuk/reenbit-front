import styled from 'styled-components'

export const StyledList = styled.ul`
  list-style: none;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  row-gap: 24px;
  column-gap: 20px;
  margin: 0px auto;
`
